// Création du tableau contenant une liste de prénoms
let prenoms = ['May', 'Razan', 'Roua', 'David', 'Eve'];

// Affichage des prénoms
for (let i = 0; i < prenoms.length; i++) {
    console.log(prenoms[i]);
}
